version: 1

app:
  name: tetris-api
  dockerfile: backend/Dockerfile
  working_dir: /app/backend
  ports:
    - 3000
  start_command: node dist/index.js
  build_command: cd backend && npx tsc

environment:
  NODE_ENV: production
  PORT: 3000

volumes:
  - name: data
    path: /data

```
